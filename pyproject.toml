# SPDX-License-Identifier: Apache-2.0
[project]
name = "aces-amf"
version = "0.1.0"
description = "ACES Metadata File (AMF) monorepo"
requires-python = ">=3.10"
dependencies = [
    "aces-aswf-amf-lib>=0.1.0",
    "aces-aswf-amf-utils>=0.1.0",
    "aces-aswf-common>=0.1.0",
    "aces-aswf-transforms>=0.1.0",
]

[tool.uv.sources]
aces-aswf-amf-lib = { workspace = true }
aces-aswf-amf-utils = { workspace = true }
aces-aswf-common = { workspace = true }
aces-aswf-transforms = { workspace = true }

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]

[tool.ruff]
line-length = 140

[tool.pytest.ini_options]
testpaths = ["packages/*/tests"]
